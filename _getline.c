#include "shell.h"

/**
 * _getline - reads text from a file stream
 * @text: pointer to buffer that will store the read text
 * @size: pointer size of the buffer text
 * @stream: file stream it reads the text from
 *
 * Return: Number of characters read
 */
ssize_t _getline(char **text, size_t *size, FILE *stream)
{
}
